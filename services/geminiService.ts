
/**
 * Serviço de IA removido a pedido do usuário.
 * Este arquivo pode ser deletado ou mantido como stub.
 */
export const analyzeSalesPerformance = async (salesData: any) => {
  return { insights: [] };
};
